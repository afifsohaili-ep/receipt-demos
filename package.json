{
  "name": "receipt-demos",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "vite build && surge dist"
  },
  "devDependencies": {
    "surge": "^0.23.0",
    "vite": "^2.6.4"
  }
}
